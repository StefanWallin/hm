{
  "name": "HM",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "watch": "jest --watch=skip"
  },
  "dependencies": {
    "react-native": "^0.20.0",
    "react-native-db-models": "^0.1.3"
  },
  "devDependencies": {
    "jest-cli": "^0.8.2"
  }
}
